Remeber href="" reloads the page necessarily.
npm i redux react-redux redux-thunk

We are going to need to prepare a lot of boiler plate - probably justified because of its usefullness in very complex projects that justifies the size of boiler plate.

Reducers : can be multiple for different responsibilities.
--
We make a store and give it the access to whole application.
--
We may have multiple reducers - what we have to do is combine all reducers and export them as a single reducer function. (Yes, reducer is a function)
We do it with a index.js file inside the reducer folder.
---
Redux thunk helps to execute asynchronous functions.
